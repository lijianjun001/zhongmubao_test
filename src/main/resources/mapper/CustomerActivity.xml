<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.zhongmubao.api.dao.CustomerActivityDao">
    <!-- 目的：为dao接口方法提供sql语句配置 -->

    <select id="getCustomerActivityById" resultType="CustomerActivity">
        <!-- 具体的sql -->
        SELECT
        *
        FROM
        CustomerActivity
        WHERE
        id = #{id} limit 1
    </select>

    <select id="pagerCustomerActivityList" resultType="CustomerActivity">
        SELECT
        *
        FROM
        CustomerActivity
        ORDER BY
        id
        LIMIT #{offset}, #{limit}
    </select>

    <insert id="insertCustomerActivity">
        <!-- ignore 主键冲突，报错 -->
        INSERT ignore INTO CustomerActivity (
			     Type, 
	     Title, 
	     Photo, 
	     Remark, 
	     Content, 
	     LibraryCount, 
	     CustomerCount, 
	     Address, 
	     BeginTime, 
	     EndTime, 
	     Deleted, 
	     Created, 
	     IsEveryone, 
	     Modified, 
	     Stock, 
	     Code 
		)
        VALUES (
			     #{type}, 
	     #{title}, 
	     #{photo}, 
	     #{remark}, 
	     #{content}, 
	     #{libraryCount}, 
	     #{customerCount}, 
	     #{address}, 
	     #{beginTime}, 
	     #{endTime}, 
	     #{deleted}, 
	     #{created}, 
	     #{isEveryone}, 
	     #{modified}, 
	     #{stock}, 
	     #{code} 
		)
    </insert>
</mapper>