<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.zhongmubao.api.dao.CustomerActivitySignDao">
    <!-- 目的：为dao接口方法提供sql语句配置 -->

    <select id="getCustomerActivitySignById" resultType="CustomerActivitySign">
        <!-- 具体的sql -->
        SELECT
        *
        FROM
        CustomerActivitySign
        WHERE
        id = #{id} limit 1
    </select>

    <select id="pagerCustomerActivitySignList" resultType="CustomerActivitySign">
        SELECT
        *
        FROM
        CustomerActivitySign
        ORDER BY
        id
        LIMIT #{offset}, #{limit}
    </select>

    <insert id="insertCustomerActivitySign">
        <!-- ignore 主键冲突，报错 -->
        INSERT ignore INTO CustomerActivitySign (
			     ActivityId, 
	     CustomerId, 
	     IsCancel, 
	     Created, 
	     Content 
		)
        VALUES (
			     #{activityId}, 
	     #{customerId}, 
	     #{isCancel}, 
	     #{created}, 
	     #{content} 
		)
    </insert>
</mapper>