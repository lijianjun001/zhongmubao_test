<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.zhongmubao.api.dao.CustomerAutoRedeemLogDao">
    <!-- 目的：为dao接口方法提供sql语句配置 -->

    <select id="getCustomerAutoRedeemLogById" resultType="CustomerAutoRedeemLog">
        <!-- 具体的sql -->
        SELECT
        *
        FROM
        CustomerAutoRedeemLog
        WHERE
        id = #{id} limit 1
    </select>

    <select id="pagerCustomerAutoRedeemLogList" resultType="CustomerAutoRedeemLog">
        SELECT
        *
        FROM
        CustomerAutoRedeemLog
        ORDER BY
        id
        LIMIT #{offset}, #{limit}
    </select>

    <insert id="insertCustomerAutoRedeemLog">
        <!-- ignore 主键冲突，报错 -->
        INSERT ignore INTO CustomerAutoRedeemLog (
        CustomerId,
        ProjectId,
        IsRead,
        Created
        )
        VALUES (
        #{customerId},
        #{projectId},
        #{isRead},
        #{created}
        )
    </insert>
</mapper>