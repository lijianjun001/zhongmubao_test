<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.zhongmubao.api.dao.CustomerBankDao">
    <!-- 目的：为dao接口方法提供sql语句配置 -->

    <select id="getCustomerBankById" resultType="CustomerBank">
        <!-- 具体的sql -->
        SELECT
        *
        FROM
        CustomerBank
        WHERE
        id = #{id} limit 1
    </select>

    <select id="pagerCustomerBankList" resultType="CustomerBank">
        SELECT
        *
        FROM
        CustomerBank
        ORDER BY
        id
        LIMIT #{offset}, #{limit}
    </select>

    <insert id="insertCustomerBank">
        <!-- ignore 主键冲突，报错 -->
        INSERT ignore INTO CustomerBank (
			     CustomerId, 
	     BankName, 
	     BankAccount, 
	     BankOwner, 
	     IsDefault, 
	     Deleted, 
	     Created, 
	     Modified 
		)
        VALUES (
			     #{customerId}, 
	     #{bankName}, 
	     #{bankAccount}, 
	     #{bankOwner}, 
	     #{isDefault}, 
	     #{deleted}, 
	     #{created}, 
	     #{modified} 
		)
    </insert>
</mapper>