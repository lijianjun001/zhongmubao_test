<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.zhongmubao.api.dao.CustomerDepositLogDao">
    <!-- 目的：为dao接口方法提供sql语句配置 -->

    <select id="getCustomerDepositLogById" resultType="CustomerDepositLog">
        <!-- 具体的sql -->
        SELECT
        *
        FROM
        CustomerDepositLog
        WHERE
        id = #{id} limit 1
    </select>

    <select id="pagerCustomerDepositLogList" resultType="CustomerDepositLog">
        SELECT
        *
        FROM
        CustomerDepositLog
        ORDER BY
        id
        LIMIT #{offset}, #{limit}
    </select>

    <insert id="insertCustomerDepositLog">
        <!-- ignore 主键冲突，报错 -->
        INSERT ignore INTO CustomerDepositLog (
        Code,
        CustomerId,
        Amount,
        State,
        PaymentTN,
        PaymentTime,
        Deleted,
        Created,
        Modified
        )
        VALUES (
        #{code},
        #{customerId},
        #{amount},
        #{state},
        #{paymentTN},
        #{paymentTime},
        #{deleted},
        #{created},
        #{modified}
        )
    </insert>
</mapper>