<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.zhongmubao.api.dao.CustomerGiftDao">
    <!-- 目的：为dao接口方法提供sql语句配置 -->

    <select id="getCustomerGiftById" resultType="CustomerGift">
        <!-- 具体的sql -->
        SELECT
        *
        FROM
        CustomerGift
        WHERE
        id = #{id} limit 1
    </select>

    <select id="pagerCustomerGiftList" resultType="CustomerGift">
        SELECT
        *
        FROM
        CustomerGift
        ORDER BY
        id
        LIMIT #{offset}, #{limit}
    </select>

    <insert id="insertCustomerGift">
        <!-- ignore 主键冲突，报错 -->
        INSERT ignore INTO CustomerGift (
        LibraryCount,
        Name,
        Icon,
        Photo,
        Ppv,
        Decription,
        Deleted,
        Created,
        Modified,
        Type,
        Price,
        Stock,
        Condition,
        Window,
        Size,
        PanicBuying,
        PanicTime,
        PanicPhoto,
        Sketch,
        IsQRCode,
        Photos,
        Sort
        )
        VALUES (
        #{libraryCount},
        #{name},
        #{icon},
        #{photo},
        #{ppv},
        #{decription},
        #{deleted},
        #{created},
        #{modified},
        #{type},
        #{price},
        #{stock},
        #{condition},
        #{window},
        #{size},
        #{panicBuying},
        #{panicTime},
        #{panicPhoto},
        #{sketch},
        #{isQRCode},
        #{photos},
        #{sort}
        )
    </insert>
</mapper>