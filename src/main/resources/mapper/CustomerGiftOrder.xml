<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.zhongmubao.api.dao.CustomerGiftOrderDao">
    <!-- 目的：为dao接口方法提供sql语句配置 -->

    <select id="getCustomerGiftOrderById" resultType="CustomerGiftOrder">
        <!-- 具体的sql -->
        SELECT
        *
        FROM
        CustomerGiftOrder
        WHERE
        id = #{id} limit 1
    </select>

    <select id="pagerCustomerGiftOrderList" resultType="CustomerGiftOrder">
        SELECT
        *
        FROM
        CustomerGiftOrder
        ORDER BY
        id
        LIMIT #{offset}, #{limit}
    </select>

    <insert id="insertCustomerGiftOrder">
        <!-- ignore 主键冲突，报错 -->
        INSERT ignore INTO CustomerGiftOrder (
			     Code, 
	     LibraryCount, 
	     CustomerGiftId, 
	     CustomerId, 
	     Count, 
	     State, 
	     Phone, 
	     Name, 
	     Address, 
	     UserId, 
	     Deleted, 
	     Created, 
	     Modified 
		)
        VALUES (
			     #{code}, 
	     #{libraryCount}, 
	     #{customerGiftId}, 
	     #{customerId}, 
	     #{count}, 
	     #{state}, 
	     #{phone}, 
	     #{name}, 
	     #{address}, 
	     #{userId}, 
	     #{deleted}, 
	     #{created}, 
	     #{modified} 
		)
    </insert>
</mapper>