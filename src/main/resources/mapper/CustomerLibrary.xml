<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.zhongmubao.api.dao.CustomerLibraryDao">
    <!-- 目的：为dao接口方法提供sql语句配置 -->

    <select id="getCustomerLibraryById" resultType="CustomerLibrary">
        <!-- 具体的sql -->
        SELECT
        *
        FROM
        CustomerLibrary
        WHERE
        id = #{id} limit 1
    </select>

    <select id="pagerCustomerLibraryList" resultType="CustomerLibrary">
        SELECT
        *
        FROM
        CustomerLibrary
        ORDER BY
        id
        LIMIT #{offset}, #{limit}
    </select>

    <insert id="insertCustomerLibrary">
        <!-- ignore 主键冲突，报错 -->
        INSERT ignore INTO CustomerLibrary (
			     CustomerId, 
	     OriginalCount, 
	     Count, 
	     Type, 
	     Remark, 
	     Created, 
	     Reason 
		)
        VALUES (
			     #{customerId}, 
	     #{originalCount}, 
	     #{count}, 
	     #{type}, 
	     #{remark}, 
	     #{created}, 
	     #{reason} 
		)
    </insert>
</mapper>