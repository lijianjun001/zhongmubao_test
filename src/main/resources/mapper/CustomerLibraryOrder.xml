<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.zhongmubao.api.dao.CustomerLibraryOrderDao">
    <!-- 目的：为dao接口方法提供sql语句配置 -->

    <select id="getCustomerLibraryOrderById" resultType="CustomerLibraryOrder">
        <!-- 具体的sql -->
        SELECT
        *
        FROM
        CustomerLibraryOrder
        WHERE
        id = #{id} limit 1
    </select>

    <select id="pagerCustomerLibraryOrderList" resultType="CustomerLibraryOrder">
        SELECT
        *
        FROM
        CustomerLibraryOrder
        ORDER BY
        id
        LIMIT #{offset}, #{limit}
    </select>

    <insert id="insertCustomerLibraryOrder">
        <!-- ignore 主键冲突，报错 -->
        INSERT ignore INTO CustomerLibraryOrder (
			     Code, 
	     CustomerId, 
	     Sign, 
	     Count, 
	     State, 
	     StoreId, 
	     StoreName, 
	     Expired, 
	     Deleted, 
	     Created, 
	     Modified 
		)
        VALUES (
			     #{code}, 
	     #{customerId}, 
	     #{sign}, 
	     #{count}, 
	     #{state}, 
	     #{storeId}, 
	     #{storeName}, 
	     #{expired}, 
	     #{deleted}, 
	     #{created}, 
	     #{modified} 
		)
    </insert>
</mapper>