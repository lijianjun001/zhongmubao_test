<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.zhongmubao.api.dao.CustomerMessageDao">
    <!-- 目的：为dao接口方法提供sql语句配置 -->

    <select id="getCustomerMessageById" resultType="CustomerMessage">
        <!-- 具体的sql -->
        SELECT
        *
        FROM
        CustomerMessage
        WHERE
        id = #{id} limit 1
    </select>

    <select id="pagerCustomerMessageList" resultType="CustomerMessage">
        SELECT
        *
        FROM
        CustomerMessage
        ORDER BY
        id
        LIMIT #{offset}, #{limit}
    </select>

    <insert id="insertCustomerMessage">
        <!-- ignore 主键冲突，报错 -->
        INSERT ignore INTO CustomerMessage (
        CustomerId,
        Title,
        Content,
        State,
        Deleted,
        Created,
        Modified
        )
        VALUES (
        #{customerId},
        #{title},
        #{content},
        #{state},
        #{deleted},
        #{created},
        #{modified}
        )
    </insert>
</mapper>