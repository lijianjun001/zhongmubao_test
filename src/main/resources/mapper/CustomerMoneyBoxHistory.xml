<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.zhongmubao.api.dao.CustomerMoneyBoxHistoryDao">
    <!-- 目的：为dao接口方法提供sql语句配置 -->

    <select id="getCustomerMoneyBoxHistoryById" resultType="CustomerMoneyBoxHistory">
        <!-- 具体的sql -->
        SELECT
        *
        FROM
        CustomerMoneyBoxHistory
        WHERE
        id = #{id} limit 1
    </select>

    <select id="pagerCustomerMoneyBoxHistoryList" resultType="CustomerMoneyBoxHistory">
        SELECT
        *
        FROM
        CustomerMoneyBoxHistory
        ORDER BY
        id
        LIMIT #{offset}, #{limit}
    </select>

    <insert id="insertCustomerMoneyBoxHistory">
        <!-- ignore 主键冲突，报错 -->
        INSERT ignore INTO CustomerMoneyBoxHistory (
        CustomerId,
        OriginalMoeny,
        AddMoney,
        Type,
        Transaction,
        Remark,
        Created
        )
        VALUES (
        #{customerId},
        #{originalMoeny},
        #{addMoney},
        #{type},
        #{transaction},
        #{remark},
        #{created}
        )
    </insert>
</mapper>