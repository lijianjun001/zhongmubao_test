<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.zhongmubao.api.dao.CustomerPackageOrderDao">
    <!-- 目的：为dao接口方法提供sql语句配置 -->

    <select id="getCustomerPackageOrderById" resultType="CustomerPackageOrder">
        <!-- 具体的sql -->
        SELECT
        *
        FROM
        CustomerPackageOrder
        WHERE
        id = #{id} limit 1
    </select>

    <select id="pagerCustomerPackageOrderList" resultType="CustomerPackageOrder">
        SELECT
        *
        FROM
        CustomerPackageOrder
        ORDER BY
        id
        LIMIT #{offset}, #{limit}
    </select>

    <insert id="insertCustomerPackageOrder">
        <!-- ignore 主键冲突，报错 -->
        INSERT ignore INTO CustomerPackageOrder (
			     Code, 
	     ProjectId, 
	     CustomerId, 
	     Count, 
	     Amount, 
	     State, 
	     Phone, 
	     Name, 
	     Address, 
	     UserId, 
	     Deleted, 
	     Created, 
	     Modified 
		)
        VALUES (
			     #{code}, 
	     #{projectId}, 
	     #{customerId}, 
	     #{count}, 
	     #{amount}, 
	     #{state}, 
	     #{phone}, 
	     #{name}, 
	     #{address}, 
	     #{userId}, 
	     #{deleted}, 
	     #{created}, 
	     #{modified} 
		)
    </insert>
</mapper>