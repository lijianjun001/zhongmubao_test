<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.zhongmubao.api.dao.CustomerPackageOrderDetailsDao">
    <!-- 目的：为dao接口方法提供sql语句配置 -->

    <select id="getCustomerPackageOrderDetailsById" resultType="CustomerPackageOrderDetails">
        <!-- 具体的sql -->
        SELECT
        *
        FROM
        CustomerPackageOrderDetails
        WHERE
        id = #{id} limit 1
    </select>

    <select id="pagerCustomerPackageOrderDetailsList" resultType="CustomerPackageOrderDetails">
        SELECT
        *
        FROM
        CustomerPackageOrderDetails
        ORDER BY
        id
        LIMIT #{offset}, #{limit}
    </select>

    <insert id="insertCustomerPackageOrderDetails">
        <!-- ignore 主键冲突，报错 -->
        INSERT ignore INTO CustomerPackageOrderDetails (
			     PackageOrderId, 
	     CustomerGiftId, 
	     Count, 
	     Amount, 
	     Deleted, 
	     Created, 
	     Modified, 
	     Title 
		)
        VALUES (
			     #{packageOrderId}, 
	     #{customerGiftId}, 
	     #{count}, 
	     #{amount}, 
	     #{deleted}, 
	     #{created}, 
	     #{modified}, 
	     #{title} 
		)
    </insert>
</mapper>