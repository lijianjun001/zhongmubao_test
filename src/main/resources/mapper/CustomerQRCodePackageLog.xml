<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.zhongmubao.api.dao.CustomerQRCodePackageLogDao">
    <!-- 目的：为dao接口方法提供sql语句配置 -->

    <select id="getCustomerQRCodePackageLogById" resultType="CustomerQRCodePackageLog">
        <!-- 具体的sql -->
        SELECT
        *
        FROM
        CustomerQRCodePackageLog
        WHERE
        id = #{id} limit 1
    </select>

    <select id="pagerCustomerQRCodePackageLogList" resultType="CustomerQRCodePackageLog">
        SELECT
        *
        FROM
        CustomerQRCodePackageLog
        ORDER BY
        id
        LIMIT #{offset}, #{limit}
    </select>

    <insert id="insertCustomerQRCodePackageLog">
        <!-- ignore 主键冲突，报错 -->
        INSERT ignore INTO CustomerQRCodePackageLog (
        CustomerId,
        Created
        )
        VALUES (
        #{customerId},
        #{created}
        )
    </insert>
</mapper>