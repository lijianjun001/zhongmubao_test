<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.zhongmubao.api.dao.CustomerSinaDao">
    <!-- 目的：为dao接口方法提供sql语句配置 -->

    <select id="getCustomerSinaById" resultType="CustomerSina">
        <!-- 具体的sql -->
        SELECT
        *
        FROM
        CustomerSina
        WHERE
        id = #{id} limit 1
    </select>

    <select id="getCustomerSinaByCustomerId" resultType="CustomerSina">
        <!-- 具体的sql -->
        SELECT
        *
        FROM
        CustomerSina
        WHERE
        customerid = #{customerId} limit 1
    </select>

    <select id="pagerCustomerSinaList" resultType="CustomerSina">
        SELECT
        *
        FROM
        CustomerSina
        ORDER BY
        id
        LIMIT #{offset}, #{limit}
    </select>

    <insert id="insertCustomerSina">
        <!-- ignore 主键冲突，报错 -->
        INSERT ignore INTO CustomerSina (
        CustomerId,
        UID,
        IsRealName,
        IsBindAuth,
        IsBindCard,
        IsSetPayPwd,
        Created,
        Modified,
        CardId
        )
        VALUES (
        #{customerId},
        #{uID},
        #{isRealName},
        #{isBindAuth},
        #{isBindCard},
        #{isSetPayPwd},
        #{created},
        #{modified},
        #{cardId}
        )
    </insert>
</mapper>