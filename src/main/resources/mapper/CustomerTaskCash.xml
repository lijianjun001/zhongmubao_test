<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.zhongmubao.api.dao.CustomerTaskCashDao">
    <!-- 目的：为dao接口方法提供sql语句配置 -->

    <select id="getCustomerTaskCashById" resultType="CustomerTaskCash">
        <!-- 具体的sql -->
        SELECT
        *
        FROM
        CustomerTaskCash
        WHERE
        id = #{id} limit 1
    </select>

    <select id="pagerCustomerTaskCashList" resultType="CustomerTaskCash">
        SELECT
        *
        FROM
        CustomerTaskCash
        ORDER BY
        id
        LIMIT #{offset}, #{limit}
    </select>

    <insert id="insertCustomerTaskCash">
        <!-- ignore 主键冲突，报错 -->
        INSERT ignore INTO CustomerTaskCash (
			     CustomerId, 
	     ProjectId, 
	     OrderId, 
	     OrderNo, 
	     Amount, 
	     InterestAmount, 
	     BankName, 
	     BankAccount, 
	     BankOwner, 
	     Deleted, 
	     Created, 
	     Modified 
		)
        VALUES (
			     #{customerId}, 
	     #{projectId}, 
	     #{orderId}, 
	     #{orderNo}, 
	     #{amount}, 
	     #{interestAmount}, 
	     #{bankName}, 
	     #{bankAccount}, 
	     #{bankOwner}, 
	     #{deleted}, 
	     #{created}, 
	     #{modified} 
		)
    </insert>
</mapper>