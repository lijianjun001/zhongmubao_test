<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.zhongmubao.api.dao.CustomerTransferDao">
    <!-- 目的：为dao接口方法提供sql语句配置 -->

    <select id="getCustomerTransferById" resultType="CustomerTransfer">
        <!-- 具体的sql -->
        SELECT
        *
        FROM
        CustomerTransfer
        WHERE
        id = #{id} limit 1
    </select>

    <select id="pagerCustomerTransferList" resultType="CustomerTransfer">
        SELECT
        *
        FROM
        CustomerTransfer
        ORDER BY
        id
        LIMIT #{offset}, #{limit}
    </select>

    <insert id="insertCustomerTransfer">
        <!-- ignore 主键冲突，报错 -->
        INSERT ignore INTO CustomerTransfer (
        OldCustomerId,
        NewCustomerId,
        CardName,
        CardNo,
        CardNoPath,
        SystemUserId,
        Remark,
        Created
        )
        VALUES (
        #{oldCustomerId},
        #{newCustomerId},
        #{cardName},
        #{cardNo},
        #{cardNoPath},
        #{systemUserId},
        #{remark},
        #{created}
        )
    </insert>
</mapper>