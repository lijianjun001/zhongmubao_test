<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.zhongmubao.api.dao.ExtActivityDao">
    <!-- 目的：为dao接口方法提供sql语句配置 -->

    <select id="getExtActivityById" resultType="ExtActivity">
        <!-- 具体的sql -->
        SELECT
        *
        FROM
        ExtActivity
        WHERE
        id = #{id} limit 1
    </select>

    <select id="pagerExtActivityList" resultType="ExtActivity">
        SELECT
        *
        FROM
        ExtActivity
        ORDER BY
        id
        LIMIT #{offset}, #{limit}
    </select>

    <insert id="insertExtActivity">
        <!-- ignore 主键冲突，报错 -->
        INSERT ignore INTO ExtActivity (
			     Title, 
	     Count, 
	     Remark, 
	     ImgUrl, 
	     LinkUrl, 
	     Sort, 
	     BeginTime, 
	     EndTime, 
	     RegistrationEndTime, 
	     CreateTime, 
	     Deleted, 
	     Type 
		)
        VALUES (
			     #{title}, 
	     #{count}, 
	     #{remark}, 
	     #{imgUrl}, 
	     #{linkUrl}, 
	     #{sort}, 
	     #{beginTime}, 
	     #{endTime}, 
	     #{registrationEndTime}, 
	     #{createTime}, 
	     #{deleted}, 
	     #{type} 
		)
    </insert>
</mapper>