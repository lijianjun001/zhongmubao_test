<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.zhongmubao.api.dao.ExtActivityRecordDao">
    <!-- 目的：为dao接口方法提供sql语句配置 -->

    <select id="getExtActivityRecordById" resultType="ExtActivityRecord">
        <!-- 具体的sql -->
        SELECT
        *
        FROM
        ExtActivityRecord
        WHERE
        id = #{id} limit 1
    </select>

    <select id="pagerExtActivityRecordList" resultType="ExtActivityRecord">
        SELECT
        *
        FROM
        ExtActivityRecord
        ORDER BY
        id
        LIMIT #{offset}, #{limit}

    </select>

    <select id="getExtActivityRecordrByCustomerIdAndActivityId" resultType="ExtActivityRecord">
         select * from ExtActivityRecord WHERE customerid = #{customerId} and ExtActivityid = #{activityId} and deleted =0 limit 1
    </select>

    <insert id="insertExtActivityRecord">
        <!-- ignore 主键冲突，报错 -->
        INSERT ignore INTO ExtActivityRecord (
			     CustomerId, 
	     ExtActivityId, 
	     Name, 
	     Phone, 
	     Remark, 
	     Created, 
	     Deleted, 
	     Info, 
	     State, 
	     AuditReason 
		)
        VALUES (
			     #{customerId}, 
	     #{extActivityId}, 
	     #{name}, 
	     #{phone}, 
	     #{remark}, 
	     #{created}, 
	     #{deleted}, 
	     #{info}, 
	     #{state}, 
	     #{auditReason} 
		)
    </insert>
</mapper>