<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.zhongmubao.api.dao.ExtBadEggLogDao">
    <!-- 目的：为dao接口方法提供sql语句配置 -->

    <select id="getExtBadEggLogById" resultType="ExtBadEggLog">
        <!-- 具体的sql -->
        SELECT
        *
        FROM
        ExtBadEggLog
        WHERE
        id = #{id} limit 1
    </select>

    <select id="pagerExtBadEggLogList" resultType="ExtBadEggLog">
        SELECT
        *
        FROM
        ExtBadEggLog
        ORDER BY
        id
        LIMIT #{offset}, #{limit}
    </select>

    <insert id="insertExtBadEggLog">
        <!-- ignore 主键冲突，报错 -->
        INSERT ignore INTO ExtBadEggLog (
			     SmashEggId, 
	     CustomerId, 
	     Created 
		)
        VALUES (
			     #{smashEggId}, 
	     #{customerId}, 
	     #{created} 
		)
    </insert>
</mapper>