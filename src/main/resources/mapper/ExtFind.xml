<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.zhongmubao.api.dao.ExtFindDao">
    <!-- 目的：为dao接口方法提供sql语句配置 -->

    <select id="getExtFindById" resultType="ExtFind">
        <!-- 具体的sql -->
        SELECT
        *
        FROM
        ExtFind
        WHERE
        id = #{id} limit 1
    </select>

    <select id="pagerExtFindList" resultType="ExtFind">
        SELECT
        *
        FROM
        ExtFind
        ORDER BY
        id
        LIMIT #{offset}, #{limit}
    </select>

    <insert id="insertExtFind">
        <!-- ignore 主键冲突，报错 -->
        INSERT ignore INTO ExtFind (
			     Title, 
	     Remark, 
	     ImgUrl, 
	     LinkUrl, 
	     Sort, 
	     BeginTime, 
	     EndTime, 
	     CreateTime, 
	     Deleted 
		)
        VALUES (
			     #{title}, 
	     #{remark}, 
	     #{imgUrl}, 
	     #{linkUrl}, 
	     #{sort}, 
	     #{beginTime}, 
	     #{endTime}, 
	     #{createTime}, 
	     #{deleted} 
		)
    </insert>
</mapper>