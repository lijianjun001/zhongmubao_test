<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.zhongmubao.api.dao.ExtOrderExpressDao">
    <!-- 目的：为dao接口方法提供sql语句配置 -->

    <select id="getExtOrderExpressById" resultType="ExtOrderExpress">
        <!-- 具体的sql -->
        SELECT
        *
        FROM
        ExtOrderExpress
        WHERE
        id = #{id} limit 1
    </select>

    <select id="pagerExtOrderExpressList" resultType="ExtOrderExpress">
        SELECT
        *
        FROM
        ExtOrderExpress
        ORDER BY
        id
        LIMIT #{offset}, #{limit}
    </select>

    <insert id="insertExtOrderExpress">
        <!-- ignore 主键冲突，报错 -->
        INSERT ignore INTO ExtOrderExpress (
			     ExpressId, 
	     OrderType, 
	     OrderId, 
	     Code, 
	     Remark, 
	     Modified, 
	     Created 
		)
        VALUES (
			     #{expressId}, 
	     #{orderType}, 
	     #{orderId}, 
	     #{code}, 
	     #{remark}, 
	     #{modified}, 
	     #{created} 
		)
    </insert>
</mapper>