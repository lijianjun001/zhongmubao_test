<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.zhongmubao.api.dao.ExtRedPackageHistoryDao">
    <!-- 目的：为dao接口方法提供sql语句配置 -->

    <select id="getExtRedPackageHistoryById" resultType="ExtRedPackageHistory">
        <!-- 具体的sql -->
        SELECT
        *
        FROM
        ExtRedPackageHistory
        WHERE
        id = #{id} limit 1
    </select>

    <select id="pagerExtRedPackageHistoryList" resultType="ExtRedPackageHistory">
        SELECT
        *
        FROM
        ExtRedPackageHistory
        ORDER BY
        id
        LIMIT #{offset}, #{limit}
    </select>

    <insert id="insertExtRedPackageHistory">
        <!-- ignore 主键冲突，报错 -->
        INSERT ignore INTO ExtRedPackageHistory (
        CustomerId,
        RedPackageId,
        Price,
        Modified,
        Created,
        Transaction
        )
        VALUES (
        #{customerId},
        #{redPackageId},
        #{price},
        #{modified},
        #{created},
        #{transaction}
        )
    </insert>
</mapper>