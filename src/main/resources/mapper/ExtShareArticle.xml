<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.zhongmubao.api.dao.ExtShareArticleDao">
    <!-- 目的：为dao接口方法提供sql语句配置 -->

    <select id="getExtShareArticleById" resultType="ExtShareArticle">
        <!-- 具体的sql -->
        SELECT
        *
        FROM
        ExtShareArticle
        WHERE
        id = #{id} limit 1
    </select>

    <select id="pagerExtShareArticleList" resultType="ExtShareArticle">
        SELECT
        *
        FROM
        ExtShareArticle
        ORDER BY
        id
        LIMIT #{offset}, #{limit}
    </select>

    <insert id="insertExtShareArticle">
        <!-- ignore 主键冲突，报错 -->
        INSERT ignore INTO ExtShareArticle (
			     CategoryId, 
	     Title, 
	     Remark, 
	     Content, 
	     Icon, 
	     Photo, 
	     AttributeValue, 
	     State, 
	     Deleted, 
	     Created, 
	     Modified 
		)
        VALUES (
			     #{categoryId}, 
	     #{title}, 
	     #{remark}, 
	     #{content}, 
	     #{icon}, 
	     #{photo}, 
	     #{attributeValue}, 
	     #{state}, 
	     #{deleted}, 
	     #{created}, 
	     #{modified} 
		)
    </insert>
</mapper>