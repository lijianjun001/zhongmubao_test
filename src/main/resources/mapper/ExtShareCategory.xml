<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.zhongmubao.api.dao.ExtShareCategoryDao">
    <!-- 目的：为dao接口方法提供sql语句配置 -->

    <select id="getExtShareCategoryById" resultType="ExtShareCategory">
        <!-- 具体的sql -->
        SELECT
        *
        FROM
        ExtShareCategory
        WHERE
        id = #{id} limit 1
    </select>

    <select id="pagerExtShareCategoryList" resultType="ExtShareCategory">
        SELECT
        *
        FROM
        ExtShareCategory
        ORDER BY
        id
        LIMIT #{offset}, #{limit}
    </select>

    <insert id="insertExtShareCategory">
        <!-- ignore 主键冲突，报错 -->
        INSERT ignore INTO ExtShareCategory (
			     Name, 
	     Template, 
	     Remark, 
	     Attribute, 
	     State, 
	     Deleted, 
	     Created, 
	     Modified 
		)
        VALUES (
			     #{name}, 
	     #{template}, 
	     #{remark}, 
	     #{attribute}, 
	     #{state}, 
	     #{deleted}, 
	     #{created}, 
	     #{modified} 
		)
    </insert>
</mapper>