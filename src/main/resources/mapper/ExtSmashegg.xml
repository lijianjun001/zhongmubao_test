<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.zhongmubao.api.dao.ExtSmasheggDao">
    <!-- 目的：为dao接口方法提供sql语句配置 -->

    <select id="getExtSmasheggById" resultType="ExtSmashegg">
        <!-- 具体的sql -->
        SELECT
        *
        FROM
        ExtSmashegg
        WHERE
        id = #{id} limit 1
    </select>

    <select id="pagerExtSmasheggList" resultType="ExtSmashegg">
        SELECT
        *
        FROM
        ExtSmashegg
        ORDER BY
        id
        LIMIT #{offset}, #{limit}
    </select>

    <insert id="insertExtSmashegg">
        <!-- ignore 主键冲突，报错 -->
        INSERT ignore INTO ExtSmashegg (
        CustomerId,
        Type,
        IsUsed,
        OrderId,
        LibraryCount,
        PrizeType,
        SmashLibraryCount,
        GiftOrderId,
        BadEggState,
        Remark,
        SmashTime,
        Deleted,
        Created,
        Modified
        )
        VALUES (
        #{customerId},
        #{type},
        #{isUsed},
        #{orderId},
        #{libraryCount},
        #{prizeType},
        #{smashLibraryCount},
        #{giftOrderId},
        #{badEggState},
        #{remark},
        #{smashTime},
        #{deleted},
        #{created},
        #{modified}
        )
    </insert>
</mapper>