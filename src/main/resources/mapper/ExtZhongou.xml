<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.zhongmubao.api.dao.ExtZhongouDao">
    <!-- 目的：为dao接口方法提供sql语句配置 -->

    <select id="getExtZhongouById" resultType="ExtZhongou">
        <!-- 具体的sql -->
        SELECT
        *
        FROM
        ExtZhongou
        WHERE
        id = #{id} limit 1
    </select>

    <select id="pagerExtZhongouList" resultType="ExtZhongou">
        SELECT
        *
        FROM
        ExtZhongou
        ORDER BY
        id
        LIMIT #{offset}, #{limit}
    </select>

    <insert id="insertExtZhongou">
        <!-- ignore 主键冲突，报错 -->
        INSERT ignore INTO ExtZhongou (
			     Code, 
	     CustomerId, 
	     Name, 
	     Phone, 
	     Address, 
	     State, 
	     PostState, 
	     Deleted, 
	     Created, 
	     Modified 
		)
        VALUES (
			     #{code}, 
	     #{customerId}, 
	     #{name}, 
	     #{phone}, 
	     #{address}, 
	     #{state}, 
	     #{postState}, 
	     #{deleted}, 
	     #{created}, 
	     #{modified} 
		)
    </insert>
</mapper>