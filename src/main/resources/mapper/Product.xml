<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.zhongmubao.api.dao.ProductDao">
    <!-- 目的：为dao接口方法提供sql语句配置 -->

    <select id="getProductById" resultType="Product">
        <!-- 具体的sql -->
        SELECT
        *
        FROM
        Product
        WHERE
        id = #{id} limit 1
    </select>

    <select id="pagerProductList" resultType="Product">
        SELECT
        *
        FROM
        Product
        ORDER BY
        id
        LIMIT #{offset}, #{limit}
    </select>

    <insert id="insertProduct">
        <!-- ignore 主键冲突，报错 -->
        INSERT ignore INTO Product (
        ProjectId,
        Title,
        SubTitle,
        Desc,
        Photo,
        Icon,
        Amount,
        MarketAmount,
        FreightAmount,
        FreeFreightCount,
        PromotionInfo,
        MaxLibrary,
        SaleCount,
        Stock,
        AttrDesc,
        Ppv,
        Deleted,
        Created,
        Modified,
        SaleTime,
        MaxBuyCount
        )
        VALUES (
        #{projectId},
        #{title},
        #{subTitle},
        #{desc},
        #{photo},
        #{icon},
        #{amount},
        #{marketAmount},
        #{freightAmount},
        #{freeFreightCount},
        #{promotionInfo},
        #{maxLibrary},
        #{saleCount},
        #{stock},
        #{attrDesc},
        #{ppv},
        #{deleted},
        #{created},
        #{modified},
        #{saleTime},
        #{maxBuyCount}
        )
    </insert>
</mapper>