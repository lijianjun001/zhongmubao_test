<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.zhongmubao.api.dao.ProductOrderDetailDao">
    <!-- 目的：为dao接口方法提供sql语句配置 -->

    <select id="getProductOrderDetailById" resultType="ProductOrderDetail">
        <!-- 具体的sql -->
        SELECT
        *
        FROM
        ProductOrderDetail
        WHERE
        id = #{id} limit 1
    </select>

    <select id="pagerProductOrderDetailList" resultType="ProductOrderDetail">
        SELECT
        *
        FROM
        ProductOrderDetail
        ORDER BY
        id
        LIMIT #{offset}, #{limit}
    </select>

    <insert id="insertProductOrderDetail">
        <!-- ignore 主键冲突，报错 -->
        INSERT ignore INTO ProductOrderDetail (
			     OrderId, 
	     ProductId, 
	     Count, 
	     Amount, 
	     Remark, 
	     Deleted, 
	     Created, 
	     Modified 
		)
        VALUES (
			     #{orderId}, 
	     #{productId}, 
	     #{count}, 
	     #{amount}, 
	     #{remark}, 
	     #{deleted}, 
	     #{created}, 
	     #{modified} 
		)
    </insert>
</mapper>