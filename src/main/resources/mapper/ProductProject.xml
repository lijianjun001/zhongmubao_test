<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.zhongmubao.api.dao.ProductProjectDao">
    <!-- 目的：为dao接口方法提供sql语句配置 -->

    <select id="getProductProjectById" resultType="ProductProject">
        <!-- 具体的sql -->
        SELECT
        *
        FROM
        ProductProject
        WHERE
        id = #{id} limit 1
    </select>

    <select id="pagerProductProjectList" resultType="ProductProject">
        SELECT
        *
        FROM
        ProductProject
        ORDER BY
        id
        LIMIT #{offset}, #{limit}
    </select>

    <insert id="insertProductProject">
        <!-- ignore 主键冲突，报错 -->
        INSERT ignore INTO ProductProject (
        Title,
        Desc,
        Amount,
        BeginTime,
        EndTime,
        Deleted,
        Created,
        Modified,
        LibraryAmount
        )
        VALUES (
        #{title},
        #{desc},
        #{amount},
        #{beginTime},
        #{endTime},
        #{deleted},
        #{created},
        #{modified},
        #{libraryAmount}
        )
    </insert>
</mapper>