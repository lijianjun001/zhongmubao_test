<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.zhongmubao.api.dao.SheepBonusDao">
    <!-- 目的：为dao接口方法提供sql语句配置 -->

    <select id="getSheepBonusById" resultType="SheepBonus">
        <!-- 具体的sql -->
        SELECT
        *
        FROM
        SheepBonus
        WHERE
        id = #{id} limit 1
    </select>

    <select id="pagerSheepBonusList" resultType="SheepBonus">
        SELECT
        *
        FROM
        SheepBonus
        ORDER BY
        id
        LIMIT #{offset}, #{limit}
    </select>

    <insert id="insertSheepBonus">
        <!-- ignore 主键冲突，报错 -->
        INSERT ignore INTO SheepBonus (
			     SenderId, 
	     SendOrderId, 
	     SendOrderNo, 
	     Receiver, 
	     ReceiverName, 
	     ReceiveOrderId, 
	     ReceiveOrderNo, 
	     Count, 
	     Amount, 
	     Remark, 
	     State, 
	     Expired, 
	     Deleted, 
	     Created, 
	     Modified 
		)
        VALUES (
			     #{senderId}, 
	     #{sendOrderId}, 
	     #{sendOrderNo}, 
	     #{receiver}, 
	     #{receiverName}, 
	     #{receiveOrderId}, 
	     #{receiveOrderNo}, 
	     #{count}, 
	     #{amount}, 
	     #{remark}, 
	     #{state}, 
	     #{expired}, 
	     #{deleted}, 
	     #{created}, 
	     #{modified} 
		)
    </insert>
</mapper>