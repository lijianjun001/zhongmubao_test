<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.zhongmubao.api.dao.SheepLevelDao">
    <!-- 目的：为dao接口方法提供sql语句配置 -->

    <select id="getSheepLevelById" resultType="SheepLevel">
        <!-- 具体的sql -->
        SELECT
        *
        FROM
        SheepLevel
        WHERE
        id = #{id} limit 1
    </select>

    <select id="pagerSheepLevelList" resultType="SheepLevel">
        SELECT
        *
        FROM
        SheepLevel
        ORDER BY
        id
        LIMIT #{offset}, #{limit}
    </select>

    <insert id="insertSheepLevel">
        <!-- ignore 主键冲突，报错 -->
        INSERT ignore INTO SheepLevel (
        Level,
        SheepCount,
        Remark,
        Deleted,
        Created,
        Modified
        )
        VALUES (
        #{level},
        #{sheepCount},
        #{remark},
        #{deleted},
        #{created},
        #{modified}
        )
    </insert>
</mapper>