<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.zhongmubao.api.dao.SheepOrderRedeemLogDao">
    <!-- 目的：为dao接口方法提供sql语句配置 -->

    <select id="getSheepOrderRedeemLogById" resultType="SheepOrderRedeemLog">
        <!-- 具体的sql -->
        SELECT
        *
        FROM
        SheepOrderRedeemLog
        WHERE
        id = #{id} limit 1
    </select>

    <select id="pagerSheepOrderRedeemLogList" resultType="SheepOrderRedeemLog">
        SELECT
        *
        FROM
        SheepOrderRedeemLog
        ORDER BY
        id
        LIMIT #{offset}, #{limit}
    </select>

    <insert id="insertSheepOrderRedeemLog">
        <!-- ignore 主键冲突，报错 -->
        INSERT ignore INTO SheepOrderRedeemLog (
			     CustomerId, 
	     ProjectId, 
	     Price, 
	     SinaInfo, 
	     CreateTime, 
	     OutTradeNo, 
	     NotifyTradeStatus 
		)
        VALUES (
			     #{customerId}, 
	     #{projectId}, 
	     #{price}, 
	     #{sinaInfo}, 
	     #{createTime}, 
	     #{outTradeNo}, 
	     #{notifyTradeStatus} 
		)
    </insert>
</mapper>