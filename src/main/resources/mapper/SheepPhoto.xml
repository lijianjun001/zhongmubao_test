<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.zhongmubao.api.dao.SheepPhotoDao">
    <!-- 目的：为dao接口方法提供sql语句配置 -->

    <select id="getSheepPhotoById" resultType="SheepPhoto">
        <!-- 具体的sql -->
        SELECT
        *
        FROM
        SheepPhoto
        WHERE
        id = #{id} limit 1
    </select>

    <select id="pagerSheepPhotoList" resultType="SheepPhoto">
        SELECT
        *
        FROM
        SheepPhoto
        ORDER BY
        id
        LIMIT #{offset}, #{limit}
    </select>

    <insert id="insertSheepPhoto">
        <!-- ignore 主键冲突，报错 -->
        INSERT ignore INTO SheepPhoto (
        ProjectId,
        OrderId,
        OrderNo,
        CustomerId,
        Photo,
        Deleted,
        Created,
        Modified
        )
        VALUES (
        #{projectId},
        #{orderId},
        #{orderNo},
        #{customerId},
        #{photo},
        #{deleted},
        #{created},
        #{modified}
        )
    </insert>
</mapper>