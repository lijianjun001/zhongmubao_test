<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.zhongmubao.api.dao.SheepProjectPlanDao">
    <!-- 目的：为dao接口方法提供sql语句配置 -->

    <select id="getSheepProjectPlanById" resultType="SheepProjectPlan">
        <!-- 具体的sql -->
        SELECT
        *
        FROM
        SheepProjectPlan
        WHERE
        id = #{id} limit 1
    </select>

    <select id="pagerSheepProjectPlanList" resultType="SheepProjectPlan">
        SELECT
        *
        FROM
        SheepProjectPlan
        ORDER BY
        id
        LIMIT #{offset}, #{limit}
    </select>

    <insert id="insertSheepProjectPlan">
        <!-- ignore 主键冲突，报错 -->
        INSERT ignore INTO SheepProjectPlan (
			     Time, 
	     Info, 
	     CreateTime 
		)
        VALUES (
			     #{time}, 
	     #{info}, 
	     #{createTime} 
		)
    </insert>
</mapper>