<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.zhongmubao.api.dao.SheepStageDao">
    <!-- 目的：为dao接口方法提供sql语句配置 -->

    <select id="getSheepStageById" resultType="SheepStage">
        <!-- 具体的sql -->
        SELECT
        *
        FROM
        SheepStage
        WHERE
        id = #{id} limit 1
    </select>

    <select id="pagerSheepStageList" resultType="SheepStage">
        SELECT
        *
        FROM
        SheepStage
        ORDER BY
        id
        LIMIT #{offset}, #{limit}
    </select>

    <insert id="insertSheepStage">
        <!-- ignore 主键冲突，报错 -->
        INSERT ignore INTO SheepStage (
        Name,
        Period,
        Icon,
        SelectedIcon,
        Remark,
        Deleted,
        Created,
        Modified
        )
        VALUES (
        #{name},
        #{period},
        #{icon},
        #{selectedIcon},
        #{remark},
        #{deleted},
        #{created},
        #{modified}
        )
    </insert>
</mapper>