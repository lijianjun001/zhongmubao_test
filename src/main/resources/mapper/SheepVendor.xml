<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.zhongmubao.api.dao.SheepVendorDao">
    <!-- 目的：为dao接口方法提供sql语句配置 -->

    <select id="getSheepVendorById" resultType="SheepVendor">
        <!-- 具体的sql -->
        SELECT
        *
        FROM
        SheepVendor
        WHERE
        id = #{id} limit 1
    </select>

    <select id="getSheepVendorList" resultType="SheepVendor">
        <!-- 具体的sql -->
        SELECT
        *
        FROM
        SheepVendor
    </select>

    <select id="pagerSheepVendorList" resultType="SheepVendor">
        SELECT
        *
        FROM
        SheepVendor
        ORDER BY
        id
        LIMIT #{offset}, #{limit}
    </select>

    <insert id="insertSheepVendor">
        <!-- ignore 主键冲突，报错 -->
        INSERT ignore INTO SheepVendor (
        Name,
        Temperature,
        Humidity,
        Remark,
        Deleted,
        Created,
        Modified,
        LoginName,
        LoginPwd,
        ContactName,
        Phone,
        CardId,
        Shorthand,
        Attrs,
        Pics
        )
        VALUES (
        #{name},
        #{temperature},
        #{humidity},
        #{remark},
        #{deleted},
        #{created},
        #{modified},
        #{loginName},
        #{loginPwd},
        #{contactName},
        #{phone},
        #{cardId},
        #{shorthand},
        #{attrs},
        #{pics}
        )
    </insert>
</mapper>