<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.zhongmubao.api.dao.SheepVendorMoneyHistoryDao">
    <!-- 目的：为dao接口方法提供sql语句配置 -->

    <select id="getSheepVendorMoneyHistoryById" resultType="SheepVendorMoneyHistory">
        <!-- 具体的sql -->
        SELECT
        *
        FROM
        SheepVendorMoneyHistory
        WHERE
        id = #{id} limit 1
    </select>

    <select id="pagerSheepVendorMoneyHistoryList" resultType="SheepVendorMoneyHistory">
        SELECT
        *
        FROM
        SheepVendorMoneyHistory
        ORDER BY
        id
        LIMIT #{offset}, #{limit}
    </select>

    <insert id="insertSheepVendorMoneyHistory">
        <!-- ignore 主键冲突，报错 -->
        INSERT ignore INTO SheepVendorMoneyHistory (
			     VendorId, 
	     Money, 
	     Transaction, 
	     SinaInfo, 
	     Remark, 
	     Created, 
	     SysUserId, 
	     State 
		)
        VALUES (
			     #{vendorId}, 
	     #{money}, 
	     #{transaction}, 
	     #{sinaInfo}, 
	     #{remark}, 
	     #{created}, 
	     #{sysUserId}, 
	     #{state} 
		)
    </insert>
</mapper>