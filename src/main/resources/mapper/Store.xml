<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.zhongmubao.api.dao.StoreDao">
    <!-- 目的：为dao接口方法提供sql语句配置 -->

    <select id="getStoreById" resultType="Store">
        <!-- 具体的sql -->
        SELECT
        *
        FROM
        Store
        WHERE
        id = #{id} limit 1
    </select>

    <select id="pagerStoreList" resultType="Store">
        SELECT
        *
        FROM
        Store
        ORDER BY
        id
        LIMIT #{offset}, #{limit}
    </select>

    <insert id="insertStore">
        <!-- ignore 主键冲突，报错 -->
        INSERT ignore INTO Store (
			     Code, 
	     Name, 
	     Password, 
	     Address, 
	     Phone, 
	     Point, 
	     MapUrl, 
	     Photo, 
	     Photos, 
	     BluetoothCode, 
	     Deleted, 
	     Created, 
	     Modified 
		)
        VALUES (
			     #{code}, 
	     #{name}, 
	     #{password}, 
	     #{address}, 
	     #{phone}, 
	     #{point}, 
	     #{mapUrl}, 
	     #{photo}, 
	     #{photos}, 
	     #{bluetoothCode}, 
	     #{deleted}, 
	     #{created}, 
	     #{modified} 
		)
    </insert>
</mapper>