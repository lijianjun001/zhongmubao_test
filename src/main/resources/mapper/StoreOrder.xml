<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.zhongmubao.api.dao.StoreOrderDao">
    <!-- 目的：为dao接口方法提供sql语句配置 -->

    <select id="getStoreOrderById" resultType="StoreOrder">
        <!-- 具体的sql -->
        SELECT
        *
        FROM
        StoreOrder
        WHERE
        id = #{id} limit 1
    </select>

    <select id="pagerStoreOrderList" resultType="StoreOrder">
        SELECT
        *
        FROM
        StoreOrder
        ORDER BY
        id
        LIMIT #{offset}, #{limit}
    </select>

    <insert id="insertStoreOrder">
        <!-- ignore 主键冲突，报错 -->
        INSERT ignore INTO StoreOrder (
        StoreId,
        CustomerId,
        Code,
        Amount,
        PayableAmount,
        PaymentAmount,
        Discount,
        CustomerDiscountAmount,
        DiscountAmount,
        PaymentStatus,
        PaymentType,
        PaymentTN,
        PaymentTime,
        Remark,
        Deleted,
        Created,
        Modified
        )
        VALUES (
        #{storeId},
        #{customerId},
        #{code},
        #{amount},
        #{payableAmount},
        #{paymentAmount},
        #{discount},
        #{customerDiscountAmount},
        #{discountAmount},
        #{paymentStatus},
        #{paymentType},
        #{paymentTN},
        #{paymentTime},
        #{remark},
        #{deleted},
        #{created},
        #{modified}
        )
    </insert>
</mapper>