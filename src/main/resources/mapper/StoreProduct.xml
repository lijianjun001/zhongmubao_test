<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.zhongmubao.api.dao.StoreProductDao">
    <!-- 目的：为dao接口方法提供sql语句配置 -->

    <select id="getStoreProductById" resultType="StoreProduct">
        <!-- 具体的sql -->
        SELECT
        *
        FROM
        StoreProduct
        WHERE
        id = #{id} limit 1
    </select>

    <select id="pagerStoreProductList" resultType="StoreProduct">
        SELECT
        *
        FROM
        StoreProduct
        ORDER BY
        id
        LIMIT #{offset}, #{limit}
    </select>

    <insert id="insertStoreProduct">
        <!-- ignore 主键冲突，报错 -->
        INSERT ignore INTO StoreProduct (
			     CategoryId, 
	     Code, 
	     Name, 
	     Price, 
	     Discount, 
	     Image, 
	     Remark, 
	     Deleted, 
	     Created, 
	     Modified 
		)
        VALUES (
			     #{categoryId}, 
	     #{code}, 
	     #{name}, 
	     #{price}, 
	     #{discount}, 
	     #{image}, 
	     #{remark}, 
	     #{deleted}, 
	     #{created}, 
	     #{modified} 
		)
    </insert>
</mapper>