<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.zhongmubao.api.dao.StoreQRCodeDao">
    <!-- 目的：为dao接口方法提供sql语句配置 -->

    <select id="getStoreQRCodeById" resultType="StoreQRCode">
        <!-- 具体的sql -->
        SELECT
        *
        FROM
        StoreQRCode
        WHERE
        id = #{id} limit 1
    </select>

    <select id="pagerStoreQRCodeList" resultType="StoreQRCode">
        SELECT
        *
        FROM
        StoreQRCode
        ORDER BY
        id
        LIMIT #{offset}, #{limit}
    </select>

    <insert id="insertStoreQRCode">
        <!-- ignore 主键冲突，报错 -->
        INSERT ignore INTO StoreQRCode (
			     CustomerId, 
	     Code, 
	     QRCode, 
	     StoreId, 
	     Expired, 
	     Deleted, 
	     Created, 
	     Modified, 
	     GiftId, 
	     LibraryCount 
		)
        VALUES (
			     #{customerId}, 
	     #{code}, 
	     #{qRCode}, 
	     #{storeId}, 
	     #{expired}, 
	     #{deleted}, 
	     #{created}, 
	     #{modified}, 
	     #{giftId}, 
	     #{libraryCount} 
		)
    </insert>
</mapper>