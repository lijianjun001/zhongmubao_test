<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.zhongmubao.api.dao.SystemAdminLogDao">
    <!-- 目的：为dao接口方法提供sql语句配置 -->

    <select id="getSystemAdminLogById" resultType="SystemAdminLog">
        <!-- 具体的sql -->
        SELECT
        *
        FROM
        SystemAdminLog
        WHERE
        id = #{id} limit 1
    </select>

    <select id="pagerSystemAdminLogList" resultType="SystemAdminLog">
        SELECT
        *
        FROM
        SystemAdminLog
        ORDER BY
        id
        LIMIT #{offset}, #{limit}
    </select>

    <insert id="insertSystemAdminLog">
        <!-- ignore 主键冲突，报错 -->
        INSERT ignore INTO SystemAdminLog (
        Platform,
        Category,
        Level,
        Message,
        Exception,
        Created
        )
        VALUES (
        #{platform},
        #{category},
        #{level},
        #{message},
        #{exception},
        #{created}
        )
    </insert>
</mapper>