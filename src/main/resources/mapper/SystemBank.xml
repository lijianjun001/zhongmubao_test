<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.zhongmubao.api.dao.SystemBankDao">
    <!-- 目的：为dao接口方法提供sql语句配置 -->

    <select id="getSystemBankById" resultType="SystemBank">
        <!-- 具体的sql -->
        SELECT
        *
        FROM
        SystemBank
        WHERE
        id = #{id} limit 1
    </select>

    <select id="pagerSystemBankList" resultType="SystemBank">
        SELECT
        *
        FROM
        SystemBank
        ORDER BY
        id
        LIMIT #{offset}, #{limit}
    </select>

    <insert id="insertSystemBank">
        <!-- ignore 主键冲突，报错 -->
        INSERT ignore INTO SystemBank (
        Code,
        Name,
        Deleted,
        Created,
        Modified
        )
        VALUES (
        #{code},
        #{name},
        #{deleted},
        #{created},
        #{modified}
        )
    </insert>
</mapper>