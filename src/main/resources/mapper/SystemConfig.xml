<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.zhongmubao.api.dao.SystemConfigDao">
    <!-- 目的：为dao接口方法提供sql语句配置 -->

    <select id="getSystemConfigById" resultType="SystemConfig">
        <!-- 具体的sql -->
        SELECT
        *
        FROM
        SystemConfig
        WHERE
        id = #{id} limit 1
    </select>

    <select id="pagerSystemConfigList" resultType="SystemConfig">
        SELECT
        *
        FROM
        SystemConfig
        ORDER BY
        id
        LIMIT #{offset}, #{limit}
    </select>

    <insert id="insertSystemConfig">
        <!-- ignore 主键冲突，报错 -->
        INSERT ignore INTO SystemConfig (
			     Category, 
	     Key, 
	     Value, 
	     Name, 
	     Deleted, 
	     Created, 
	     Modified 
		)
        VALUES (
			     #{category}, 
	     #{key}, 
	     #{value}, 
	     #{name}, 
	     #{deleted}, 
	     #{created}, 
	     #{modified} 
		)
    </insert>
</mapper>