<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.zhongmubao.api.dao.SystemDistrictDao">
    <!-- 目的：为dao接口方法提供sql语句配置 -->

    <select id="getSystemDistrictById" resultType="SystemDistrict">
        <!-- 具体的sql -->
        SELECT
        *
        FROM
        SystemDistrict
        WHERE
        id = #{id} limit 1
    </select>

    <select id="pagerSystemDistrictList" resultType="SystemDistrict">
        SELECT
        *
        FROM
        SystemDistrict
        ORDER BY
        id
        LIMIT #{offset}, #{limit}
    </select>

    <insert id="insertSystemDistrict">
        <!-- ignore 主键冲突，报错 -->
        INSERT ignore INTO SystemDistrict (
			     Code, 
	     ParentCode, 
	     Name, 
	     Deleted, 
	     Created, 
	     Modified 
		)
        VALUES (
			     #{code}, 
	     #{parentCode}, 
	     #{name}, 
	     #{deleted}, 
	     #{created}, 
	     #{modified} 
		)
    </insert>
</mapper>