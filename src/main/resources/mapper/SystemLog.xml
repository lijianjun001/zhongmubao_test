<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.zhongmubao.api.dao.SystemLogDao">
    <!-- 目的：为dao接口方法提供sql语句配置 -->

    <select id="getSystemLogById" resultType="SystemLog">
        <!-- 具体的sql -->
        SELECT
        *
        FROM
        SystemLog
        WHERE
        id = #{id} limit 1
    </select>

    <select id="pagerSystemLogList" resultType="SystemLog">
        SELECT
        *
        FROM
        SystemLog
        ORDER BY
        id
        LIMIT #{offset}, #{limit}
    </select>

    <insert id="insertSystemLog">
        <!-- ignore 主键冲突，报错 -->
        INSERT ignore INTO SystemLog (
        Platform,
        Category,
        Level,
        Message,
        Exception,
        Created
        )
        VALUES (
        #{platform},
        #{category},
        #{level},
        #{message},
        #{exception},
        #{created}
        )
    </insert>
</mapper>