<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.zhongmubao.api.dao.SystemMessageTemplateDao">
    <!-- 目的：为dao接口方法提供sql语句配置 -->

    <select id="getSystemMessageTemplateById" resultType="SystemMessageTemplate">
        <!-- 具体的sql -->
        SELECT
        *
        FROM
        SystemMessageTemplate
        WHERE
        id = #{id} limit 1
    </select>

    <select id="pagerSystemMessageTemplateList" resultType="SystemMessageTemplate">
        SELECT
        *
        FROM
        SystemMessageTemplate
        ORDER BY
        id
        LIMIT #{offset}, #{limit}
    </select>

    <insert id="insertSystemMessageTemplate">
        <!-- ignore 主键冲突，报错 -->
        INSERT ignore INTO SystemMessageTemplate (
        Category,
        Code,
        Template,
        Deleted,
        Created,
        Modified
        )
        VALUES (
        #{category},
        #{code},
        #{template},
        #{deleted},
        #{created},
        #{modified}
        )
    </insert>
</mapper>