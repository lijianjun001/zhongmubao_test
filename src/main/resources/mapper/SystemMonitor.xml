<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.zhongmubao.api.dao.SystemMonitorDao">
    <!-- 目的：为dao接口方法提供sql语句配置 -->

    <select id="getSystemMonitorById" resultType="SystemMonitor">
        <!-- 具体的sql -->
        SELECT
        *
        FROM
        SystemMonitor
        WHERE
        id = #{id} limit 1
    </select>

    <select id="pagerSystemMonitorList" resultType="SystemMonitor">
        SELECT
        *
        FROM
        SystemMonitor
        ORDER BY
        id
        LIMIT #{offset}, #{limit}
    </select>

    <insert id="insertSystemMonitor">
        <!-- ignore 主键冲突，报错 -->
        INSERT ignore INTO SystemMonitor (
        Type,
        DeviceId,
        ShareId,
        UKey,
        State,
        Deleted,
        Created,
        Modified
        )
        VALUES (
        #{type},
        #{deviceId},
        #{shareId},
        #{uKey},
        #{state},
        #{deleted},
        #{created},
        #{modified}
        )
    </insert>
</mapper>