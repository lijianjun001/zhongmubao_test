<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.zhongmubao.api.dao.SystemNoticeDao">
    <!-- 目的：为dao接口方法提供sql语句配置 -->

    <select id="getSystemNoticeById" resultType="SystemNotice">
        <!-- 具体的sql -->
        SELECT
        *
        FROM
        SystemNotice
        WHERE
        id = #{id} limit 1
    </select>

    <select id="pagerSystemNoticeList" resultType="SystemNotice">
        SELECT
        *
        FROM
        SystemNotice
        ORDER BY
        id
        LIMIT #{offset}, #{limit}
    </select>

    <insert id="insertSystemNotice">
        <!-- ignore 主键冲突，报错 -->
        INSERT ignore INTO SystemNotice (
        Title,
        Remark,
        Photo,
        Content,
        State,
        Deleted,
        Created,
        Modified
        )
        VALUES (
        #{title},
        #{remark},
        #{photo},
        #{content},
        #{state},
        #{deleted},
        #{created},
        #{modified}
        )
    </insert>
</mapper>