<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.zhongmubao.api.dao.SystemTaskLogDao">
    <!-- 目的：为dao接口方法提供sql语句配置 -->

    <select id="getSystemTaskLogById" resultType="SystemTaskLog">
        <!-- 具体的sql -->
        SELECT
        *
        FROM
        SystemTaskLog
        WHERE
        id = #{id} limit 1
    </select>

    <select id="pagerSystemTaskLogList" resultType="SystemTaskLog">
        SELECT
        *
        FROM
        SystemTaskLog
        ORDER BY
        id
        LIMIT #{offset}, #{limit}
    </select>

    <insert id="insertSystemTaskLog">
        <!-- ignore 主键冲突，报错 -->
        INSERT ignore INTO SystemTaskLog (
        Platform,
        Category,
        Level,
        Message,
        Exception,
        Created
        )
        VALUES (
        #{platform},
        #{category},
        #{level},
        #{message},
        #{exception},
        #{created}
        )
    </insert>
</mapper>