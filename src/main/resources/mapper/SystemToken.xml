<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.zhongmubao.api.dao.SystemTokenDao">
    <!-- 目的：为dao接口方法提供sql语句配置 -->

    <select id="getSystemTokenById" resultType="SystemToken">
        <!-- 具体的sql -->
        SELECT
        *
        FROM
        SystemToken
        WHERE
        id = #{id} limit 1
    </select>

    <select id="pagerSystemTokenList" resultType="SystemToken">
        SELECT
        *
        FROM
        SystemToken
        ORDER BY
        id
        LIMIT #{offset}, #{limit}
    </select>

    <insert id="insertSystemToken">
        <!-- ignore 主键冲突，报错 -->
        INSERT ignore INTO SystemToken (
        Platform,
        CustomerId,
        Token,
        Expired,
        Created,
        Modified
        )
        VALUES (
        #{platform},
        #{customerId},
        #{token},
        #{expired},
        #{created},
        #{modified}
        )
    </insert>
</mapper>