<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.zhongmubao.api.dao.SystemUserDao">
    <!-- 目的：为dao接口方法提供sql语句配置 -->

    <select id="getSystemUserById" resultType="SystemUser">
        <!-- 具体的sql -->
        SELECT
        *
        FROM
        SystemUser
        WHERE
        id = #{id} limit 1
    </select>

    <select id="pagerSystemUserList" resultType="SystemUser">
        SELECT
        *
        FROM
        SystemUser
        ORDER BY
        id
        LIMIT #{offset}, #{limit}
    </select>

    <insert id="insertSystemUser">
        <!-- ignore 主键冲突，报错 -->
        INSERT ignore INTO SystemUser (
        Account,
        Password,
        Name,
        Phone,
        Deleted,
        Created,
        Modified
        )
        VALUES (
        #{account},
        #{password},
        #{name},
        #{phone},
        #{deleted},
        #{created},
        #{modified}
        )
    </insert>
</mapper>