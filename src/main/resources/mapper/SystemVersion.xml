<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.zhongmubao.api.dao.SystemVersionDao">
    <!-- 目的：为dao接口方法提供sql语句配置 -->

    <select id="getSystemVersionById" resultType="SystemVersion">
        <!-- 具体的sql -->
        SELECT
        *
        FROM
        SystemVersion
        WHERE
        id = #{id} limit 1
    </select>

    <select id="pagerSystemVersionList" resultType="SystemVersion">
        SELECT
        *
        FROM
        SystemVersion
        ORDER BY
        id
        LIMIT #{offset}, #{limit}
    </select>

    <insert id="insertSystemVersion">
        <!-- ignore 主键冲突，报错 -->
        INSERT ignore INTO SystemVersion (
        Platform,
        Name,
        Version,
        ForcedUpdate,
        ChangeLogs,
        DownloadUrl,
        Deleted,
        Created,
        Modified
        )
        VALUES (
        #{platform},
        #{name},
        #{version},
        #{forcedUpdate},
        #{changeLogs},
        #{downloadUrl},
        #{deleted},
        #{created},
        #{modified}
        )
    </insert>
</mapper>