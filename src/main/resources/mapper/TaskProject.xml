<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.zhongmubao.api.dao.TaskProjectDao">
    <!-- 目的：为dao接口方法提供sql语句配置 -->

    <select id="getTaskProjectById" resultType="TaskProject">
        <!-- 具体的sql -->
        SELECT
        *
        FROM
        TaskProject
        WHERE
        id = #{id} limit 1
    </select>

    <select id="pagerTaskProjectList" resultType="TaskProject">
        SELECT
        *
        FROM
        TaskProject
        ORDER BY
        id
        LIMIT #{offset}, #{limit}
    </select>

    <insert id="insertTaskProject">
        <!-- ignore 主键冲突，报错 -->
        INSERT ignore INTO TaskProject (
			     Type, 
	     Name, 
	     Period, 
	     Template, 
	     ReleaseTime, 
	     Released, 
	     Deleted, 
	     Created, 
	     Modified 
		)
        VALUES (
			     #{type}, 
	     #{name}, 
	     #{period}, 
	     #{template}, 
	     #{releaseTime}, 
	     #{released}, 
	     #{deleted}, 
	     #{created}, 
	     #{modified} 
		)
    </insert>
</mapper>