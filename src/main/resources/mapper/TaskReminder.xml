<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.zhongmubao.api.dao.TaskReminderDao">
    <!-- 目的：为dao接口方法提供sql语句配置 -->

    <select id="getTaskReminderById" resultType="TaskReminder">
        <!-- 具体的sql -->
        SELECT
        *
        FROM
        TaskReminder
        WHERE
        id = #{id} limit 1
    </select>

    <select id="pagerTaskReminderList" resultType="TaskReminder">
        SELECT
        *
        FROM
        TaskReminder
        ORDER BY
        id
        LIMIT #{offset}, #{limit}
    </select>

    <insert id="insertTaskReminder">
        <!-- ignore 主键冲突，报错 -->
        INSERT ignore INTO TaskReminder (
			     Type, 
	     Mode, 
	     Interval, 
	     Deleted, 
	     Created, 
	     Modified 
		)
        VALUES (
			     #{type}, 
	     #{mode}, 
	     #{interval}, 
	     #{deleted}, 
	     #{created}, 
	     #{modified} 
		)
    </insert>
</mapper>